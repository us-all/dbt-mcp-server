# Contributing

Keep changes focused and include tests for behavior changes when practical.

Before submitting:

- Run `pnpm run build`.
- Run `pnpm test`.
- Update `README.md` or `server.json` when tool behavior, environment variables, or package metadata changes.
